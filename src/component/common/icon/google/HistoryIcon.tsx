import React from 'react';

const HistoryIcon = () => {
    return (
        <span className="material-symbols-outlined">
                history
        </span>
    );
};

export default HistoryIcon;